export const selectIsDetailViewOpen = (state) => state.detailView.isDetailViewOpen;
export const selectDetailViewDocument = (state) => state.detailView.document;
export const selectDetailViewDocumentID = (state) => state.detailView.documentID;
export const selectCommentData = (state) => state.detailView.commentData;
