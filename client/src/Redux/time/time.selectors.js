export const selectEndTime = (state) => state.time.endTime;
export const selectStartTime = (state) => state.time.startTime;

export const selectIntermediateEndTime = (state) => state.time.intermediateEndTime;
export const selectIntermediateStartTime = (state) => state.time.intermediateStartTime;

export const selectTimeSlice = (state) => state.time;
