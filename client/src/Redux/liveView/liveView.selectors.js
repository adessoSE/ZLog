export const selectLiveViewActive = (state) => state.liveView.active;
export const selectLiveViewPaused = (state) => state.liveView.paused;
export const selectLiveViewUnit = (state) => state.liveView.unit;
export const selectLiveViewUpdateFrequency = (state) => state.liveView.updateFrequency;
